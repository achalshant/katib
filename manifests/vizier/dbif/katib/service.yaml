apiVersion: v1
kind: Service
metadata:
  name: dbif-katib
  namespace: kubeflow
  labels:
    app: vizier
    component: dbif-katib
spec:
  type: ClusterIP
  ports:
    - port: 6789
      protocol: TCP
      name: api
  selector:
    app: vizier
    component: dbif-katib
